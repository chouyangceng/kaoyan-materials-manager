# 安卓端与 Windows 端同步协议（当前方案）

## 同步包

文件名：`考研资料管家同步包.json`

同步包包含：资料条目、学习进度、复盘记录和更新日志；附件只保存元数据，不直接包含大型文件本体。
AI 面板的 API 地址、模型和 Key 不进入同步包，默认只保存在当前设备本机。
资料条目会同步 `层级` 和 `标签` 字段，便于两端保持同一套归类逻辑。当前页面还支持多选、批量改层级、批量改标签和目录树侧栏，这些都属于本地交互，不需要额外协议。

## 推荐流程

安卓新增资料 → 录入附件元数据 → 导出同步包 → 放入云盘/发送到 Windows → Windows 导入并整理 → Windows 再导出 → 安卓导入。

## 附件同步

附件放在资料库的实际目录中。推荐使用同一个云盘文件夹同步附件，JSON 中保存相对路径或附件名，例如：

`03_英语/阅读/英语_阅读_2019真题.pdf`

## 冲突处理

- 只新增一端记录：直接合并。
- 两端修改同一记录：以最后修改时间较新的版本为主，并保留冲突副本。
- 删除：先标记为归档，不做立即物理删除。

## 当前实现

安卓端与 Windows 端现在使用同一套页面和数据结构，均支持新增资料、进度、复盘、导出同步包和导入同步包，也支持记录 Word、PPT、PDF、图片等附件元数据，并可调用 DeepSeek 生成思维导图和复习提纲。资料条目支持修改层级与标签，并带有基本分类统计、多选批量改归类和目录树侧栏。导入时按资料名称合并，较新的字段覆盖旧字段；导入后会追加更新日志。

## 当前限制

同步包仍需通过云盘、聊天工具或文件传输发送；附件文件本身仍需单独同步。真正的自动云端同步、附件路径自动对齐、批量按科目归类、AI 结果自动归档和更精细的冲突合并属于后续版本。
