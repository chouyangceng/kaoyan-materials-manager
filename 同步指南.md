# 安卓—Windows 同步指南

## 推荐方案：云盘同步（最省心）

适合希望“登录同一账号即可同步”的情况。可使用 OneDrive、Google Drive、Dropbox 或其他同时支持 Android 和 Windows 的云盘。

### 设置方式

1. 在 Windows 安装并登录云盘客户端。
2. 将整个 `考研电子资料管理系统` 文件夹放入云盘同步目录。
3. 在安卓安装同一云盘 App，并登录同一账号。
4. 安卓端打开文件后，优先使用“离线保存/下载”功能，编辑完成后等待上传完成。
5. Windows 端等待同步图标显示完成，再进行整理和备份。

### 建议

- Windows 端保留完整本地副本，作为主资料库。
- 大型视频、压缩包可以只在线保存，避免占满手机空间。
- 不要同时在安卓和 Windows 编辑同一个 Markdown 或 CSV 文件。
- 每周将整个资料库复制一份到移动硬盘或其他备份位置。

## 备选方案：局域网直连同步

如果不希望使用云盘，可以使用支持 Android 和 Windows 的文件夹同步工具，让两台设备在同一 Wi‑Fi 下直接同步。建议设置为“单向：安卓 → Windows”或“Windows 为主、安卓只上传”，以降低误删和冲突风险。

## 同步规则

- Windows 是主资料库，安卓用于查看、扫描、录入和临时整理。
- 安卓新产生的资料先放入 `00_收件箱`，同步到 Windows 后再归类。
- 文件名尽量使用中文、数字、下划线，避免特殊符号。
- 同步发生冲突时，不要直接覆盖；先保留两个版本，再人工合并。
- 资料库中的 `资料索引.csv` 和 `README.md` 尽量只在 Windows 端修改。

## 第一次同步检查

- [ ] Windows 已安装云盘客户端并登录
- [ ] 整个资料库已放入同步目录
- [ ] 安卓可以看到 `README.md` 和各科目文件夹
- [ ] 安卓创建一个测试文件到 `00_收件箱`
- [ ] Windows 能看到测试文件
- [ ] 确认 Windows 本地有完整副本

